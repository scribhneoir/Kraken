<div class="col">
    <button style="background-image: url({$playlistImage1}), 
        url({$playlistImage2}),
        url({$playlistImage3}), 
        url({$playlistImage4});" type="button" class="btn collapsible">
    </button>
    <p>{$playlistTitle}</p>
</div>